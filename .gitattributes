# Shell scripts must stay LF so they run on Linux nodes.
*.sh text eol=lf
install-server.sh text eol=lf
scripts/*.sh text eol=lf

# Go sources normalize to LF.
*.go text eol=lf
go.mod text eol=lf

# Windows scripts keep CRLF.
*.ps1 text eol=crlf
